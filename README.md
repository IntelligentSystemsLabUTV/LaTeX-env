# LaTeX-env

LaTeX environment in a Docker container.

This repository holds the source files to build the environment, on which the parent template repository [LaTeX-template](https://github.com/IntelligentSystemsLabUTV/LaTeX-template) is based.
